import React from 'react'

import { AppContainer, AppTitle, AppSubtitle } from './styles'

const App = () => {
  return (
    <AppContainer>
      <AppTitle className="heading">React Boilerplate</AppTitle>
      <AppSubtitle className="sub-heading">
        Now we can start out the project
      </AppSubtitle>
    </AppContainer>
  )
}

export default App
